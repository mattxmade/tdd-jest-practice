A caesarCipher function that takes a string and returns it with each character “shifted”.
Add a second parameter that accepts a shitf amount.

Test wrapping from z to a.
Test keeping the same case.
Test punctuation!
Test for errors.

Split the final function into smaller functions. 

Test the final caesarCipher function; if it works as expected you can rest assured 
that your smaller helper functions are doing what they’re supposed to.